# FocusTranslator v0.5.6 実装計画書（implement）

対応要件: [`v0.5.6-req.md`](./v0.5.6-req.md)

作成日: 2026-08-10 / ステータス: **追加要望を実装中**
ブランチ: `feature/v0.5.6`

> 本ドキュメントは、ログイン済みのLLM CLIを既存のLLMプロファイルへ統合し、OCR・翻訳・解説に利用するための実装計画兼報告書である。実装の進行に合わせて更新する。

---

## 0. 事前確認（ユーザー回答・実測結果）

### 0-1. 実装前に確認した仕様

| 論点 | 決定 |
| :--- | :--- |
| UI上の位置付け | CLI専用画面を分離せず、既存のLLMプロファイル種別として追加する。既存のプロファイル別OCR・翻訳・解説チップ、プロンプト、キャッシュ、ログをそのまま利用できるため。 |
| 導入と認証 | PATH自動検出、手動パス、接続確認、案内を実装する。第三者CLIのインストールやOAuthログインは外部状態を大きく変更するため自動化しない。 |
| モデル | `model_name` はCLIでも共用し、空欄ならCLI既定値を使う。Kimi CLIの新規既定プロファイルのみ `k3` とする。 |
| 実行権限 | リクエスト単位の一時ディレクトリを作業ディレクトリとし、各CLIで利用可能なread-only／ツール制限オプションを付ける。シェル文字列は組み立てず、実行ファイルと引数を分離して起動する。 |
| 失敗処理 | 120秒で子プロセスを終了し、stderr・終了コードをマスクしたエラーとして返す。別エンジンへの暗黙フォールバックは行わない。 |
| ログ | CLIが構造化出力でusageを返す場合だけtokens_in/outを保存する。取得不能時はNULLとする。 |

### 0-2. 公式仕様とローカル実測

- Codex CLI: `codex exec` が非対話実行、`--image` が画像添付、`--output-last-message` が最終応答のファイル出力に対応。
- Claude Code: `claude -p` が非対話実行、`--output-format json` が構造化出力に対応。画像はプロンプト内でパスを指定して読ませられる。
- GitHub Copilot CLI: `copilot -p` がプログラム実行に対応し、`-s` で付加表示を抑制できる。画像対応モデルでは画像ファイルを添付できる。
- Gemini CLI: `gemini -p` が非対話実行、`--output-format json` が構造化出力に対応。組み込みread_fileは画像を扱える。
- Kimi CLI: `kimi -p` が非対話実行、`--output-format` が出力形式指定に対応。K3は画像入力に対応し、ReadMediaFileで画像を読める。
- ローカル環境ではClaude Code 2.1.197を検出。初回実装時はCodexを開発サンドボックスから起動できなかったが、導入完了後に通常ユーザー環境でCodex CLI 0.147.0とChatGPTログインを確認した。Copilot/Gemini/Kimiは未導入のため、全CLIのコマンド構築・解析は単体テストし、Codexのみ実アカウントprobeも実施した。

## 1. 実装順序とコミット単位

| # | 内容 | 対象ファイル |
| :--- | :--- | :--- |
| 1 | CLI種別・設定値・安全な子プロセス実行・出力解析・タイムアウトの基盤 | `src/config.rs`, `src/llm_cli.rs`, `src/llm_api.rs`, `src/main.rs` |
| 2 | LLMプロファイル設定UIへCLI実行ファイル・モデル・状態確認を統合 | `src/settings.rs`, `src/config.rs` |
| 3 | OCR・翻訳・解説、キャッシュ、ログとの統合と回帰テスト | `src/ocr.rs`, `src/translate.rs`, `src/worker.rs`, `src/llm_api.rs`, `src/llm_cli.rs` |
| 4 | 利用者向けドキュメント、要求書、実装報告の同期 | `README.md`, `FocusTranslator_AI_CONTEXT.md`, `dev/CHANGE_LOG/v0.5.6-*.md` |

各フェーズで `cargo test` と `cargo clippy --all-targets` を通し、問題がなければ上記単位でコミットして次へ進む。

## 2. 要件別・実装方針

### 2-1. 定額プランを利用するLLM CLI実行機能

> 既存のLLM APIと同様に、Codex、Claude、GitHub Copilot、Gemini CLI、Kimi K3をCLIで呼び出し、OCR・翻訳・解説を実行する。

| 項目 | 内容 |
| :--- | :--- |
| 実装箇所 | `src/config.rs`, `src/llm_cli.rs`（新規）, `src/llm_api.rs`, `src/settings.rs` |
| プロファイル | `ApiType`へCLI5種を追加し、`ApiProfile`へ実行ファイルパスを追加する。API URL／APIキーはCLI種別では使用しない。 |
| 実行方式 | `std::process::Command`へ引数を個別設定し、シェルを介さない。PNGがある場合だけ一時ファイル化し、CLI別の公式な非対話引数で呼ぶ。stdout/stderrをUTF-8として回収する。 |
| 安全性 | 一時ディレクトリをcwdにし、Codexはread-only sandbox、Claudeは読み取り系ツールだけ、他CLIも利用可能な承認／権限制限を適用する。一時ファイルは処理後に削除する。 |
| タイムアウト | 標準ライブラリで子プロセスを監視し、120秒経過時にkillして待機・回収する。 |
| 出力 | CLI別にJSON／JSONL／テキストから最終本文とusageを抽出し、既存の`LlmResponse`へ正規化する。 |
| キャッシュ | APIリクエストJSONの代わりに、CLI種別・モデル・プロンプト・画像内容を含む安定した疑似リクエストJSONを生成し、既存DBキャッシュキーを維持する。 |
| UI | 種別選択にCLI5種を追加。CLI時はURL／APIキーを無効化し、実行ファイル欄・検出状態・接続確認・案内を表示する。 |
| 既存機能との衝突 | APIプロファイルは従来の挙動を保持する。CLIプロファイルも`is_ready`／`is_external`を共通利用し、チップ列・同意・ログの分岐漏れを防ぐ。 |

### 2-2. 利用可能モデルの選択UI

> API／CLIで利用可能なモデルを取得して編集可能なプルダウンへ表示し、CLIの既定値を軽量モデルにする。

| 項目 | 内容 |
| :--- | :--- |
| モデル欄 | Win32の編集可能コンボボックスへ変更し、一覧選択と自由入力を両立する。 |
| API一覧 | OpenAI互換 `/models`、Gemini ListModels、Claude Models APIを種別ごとに呼び分ける。 |
| CLI一覧 | CLIが機械可読な一覧を提供する場合は実行結果を解析し、提供しない場合は公式のモデル候補を表示する。 |
| 軽量既定値 | Codex=`gpt-5.6-luna`、Claude=`haiku`、Copilot=`claude-haiku-4.5`、Gemini=`gemini-2.5-flash-lite`、Kimi=`k3-256k`。 |
| 無効化 | CLIではAPIキー／最大Token、APIではCLI専用項目など、種別上使用しない入力を無効表示する。 |
| 失敗時 | 一覧取得に失敗しても現在の自由入力値を消さず、エラーを状態欄へ表示する。 |

## 3. 影響範囲

| ファイル | 変更概要 |
| :--- | :--- |
| `src/config.rs` | CLI種別、実行ファイルパス、既定プロファイル、準備済み／外部送信判定 |
| `src/llm_cli.rs` | CLI検出、引数構築、子プロセス管理、一時PNG、出力解析 |
| `src/llm_api.rs` | API／CLIの共通ディスパッチとキャッシュ用リクエスト表現 |
| `src/settings.rs` | CLIプロファイル編集・接続確認・表示切替 |
| `src/main.rs` | 新規モジュール登録 |
| `src/ocr.rs`, `src/translate.rs`, `src/worker.rs` | 共通ディスパッチ経由の回帰確認、必要なエラー／ログ調整 |
| `README.md`, `FocusTranslator_AI_CONTEXT.md` | 利用方法・構成・制限の説明 |

## 4. 実装で判明した追加対応

- npm等で導入されるCLIはWindows上で `.cmd` になる場合がある。OCR対象文字列やプロンプトを
  コマンド引数へ直接渡すと、バッチファイルを起動する層で引用符やメタ文字が再解釈される
  余地があるため、Codex/Claude/Gemini/Kimiはstdin、Copilotは一時`request.txt`から
  read-onlyで読み取る方式へ変更した。モデル名はCLI引数として必要なため、英数字と
  `- _ . : /` のみに制限する。

## 5. 進捗

- [x] 1. CLI実行基盤
- [x] 2. LLMプロファイル設定UI統合
- [x] 3. OCR・翻訳・解説・ログ統合
- [x] 4. ドキュメント同期
- [ ] 5. モデル一覧取得・編集可能プルダウン・軽量既定値

### コミット

| コミット | 内容 |
| :--- | :--- |
| `0b3ef7c` | CLI種別・設定移行・実行基盤・API共通経路・設定UIの追加 |
| `05cc628` | ユーザー入力のstdin／一時ファイル隔離、モデル名検証、CLI出力・OCR JSON解析テストの強化 |
| `b715b19` | Codex CLI 0.147.0のグローバル引数順序、ephemeral実行、実アカウントprobeの追加 |

## 6. 検証結果

### 6-1. 自動テスト・ビルド

| 対象 | 結果 |
| :--- | :--- |
| `cargo test` | 成功。68件中65件成功、明示実行用probe 3件はignored、失敗0件 |
| `cargo clippy --all-targets -- -D warnings` | 成功。警告0件 |
| `cargo build` | 成功。`target/debug/focus-translator.exe` を起動しプロセス存在まで確認 |

### 6-2. 実機・実データでの確認

- Claude Code 2.1.197の実行ファイル検出とバージョン取得に成功した。ただし、この環境のClaudeアカウントは未ログインのためモデル呼び出しは未実施。
- Codex CLI 0.147.0で `Logged in using ChatGPT` を確認し、FocusTranslatorの `llm_api::call` → `llm_cli::call` 共通経路から実アカウントprobeを実施した。翻訳は「運用テストに成功しました。」、解説はCPUの短い日本語説明、生成画像のOCRは `FOCUS OCR 42` を取得し、3件すべて成功した。
- 実測により `--ask-for-approval never` は `exec` より前に置く必要があることを確認して修正した。一時ディレクトリ用の `--skip-git-repo-check` に加え、セッションを保存しない `--ephemeral` も指定した。
- Copilot/Gemini/Kimiはローカル未導入のため、実CLIではなくコマンド構築・権限制限・出力解析の単体テストで確認した。
- アプリを最新デバッグビルドで再起動し、常駐プロセスを確認した。

> ⚠ 未実施予定の確認:
> - ローカル未導入のCopilot/Gemini/Kimi CLIによる実アカウント疎通
> - 各定額プランの残量／上限到達時のサービス固有メッセージ確認

## 7. 残件・申し送り

- Copilot/Gemini/Kimi/Claudeの実アカウントE2E確認は、各CLIを導入・ログインした環境で行う。特にCLI更新で非対話引数や構造化出力が変更された場合は `src/llm_cli.rs` の解析テストも更新する。
- 「定額プラン内」は各社CLIの認証・契約・利用上限に従う。FocusTranslatorは追加課金の有無や残量を判定しない。
- Cargo／インストーラに記録された製品バージョンは、本機能実装とは別のリリース準備作業でv0.5.6へ更新する。
